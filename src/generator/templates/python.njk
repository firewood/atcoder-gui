import sys
from typing import List, Optional

# Increase recursion depth for DFS/DP
sys.setrecursionlimit(2000000)

def input_tokens():
    for line in sys.stdin:
        for word in line.split():
            yield word

tokens = input_tokens()

{% if mod -%}
MOD = {{ mod }}
{% endif -%}
{% if yes_str -%}
YES = "{{ yes_str }}"
{% endif -%}
{% if no_str -%}
NO = "{{ no_str }}"
{% endif -%}

{% if prediction_success -%}
def solve({{ formal_arguments }}):
    # return_type: {{ return_type }}
    {% if query_cases -%}
    for _ in range({{ query_loop_var }}):
        t = int(next(tokens))
        if t == 1:
            pass
        elif t == 2:
            pass
        elif t == 3:
            pass
    {%- else -%}
    pass
    {%- endif %}
{% endif -%}

# Generated by atcoder-gui {{ tools.version }} (tips: You use the default template now. You can remove this line by using your custom template)
def main():
{% if prediction_success -%}
{% if multiple_cases -%}
    line = sys.stdin.readline()
    if not line:
        return
    t = int(line)
    for _ in range(t):
{{ input_part | safe }}
        {% if return_type == "void" -%}
        solve({{ actual_arguments }})
        {%- elif multiple_rows -%}
        ans = solve({{ actual_arguments }})
        {% if return_type == "bool" -%}
        for a in ans:
            print(YES if a else NO)
        {%- else -%}
        if ans:
            print(*ans, sep='\n')
        {%- endif %}
        {%- elif multiple_columns -%}
        ans = solve({{ actual_arguments }})
        {% if return_type == "bool" -%}
        print(*(YES if a else NO for a in ans))
        {%- else -%}
        print(*ans)
        {%- endif %}
        {%- else -%}
        ans = solve({{ actual_arguments }})
        {% if return_type == "bool" -%}
        print(YES if ans else NO)
        {%- else -%}
        print(ans)
        {%- endif %}
        {%- endif %}
{% else -%}
{{ input_part | safe }}
    {% if return_type == "void" -%}
    solve({{ actual_arguments }})
    {%- elif multiple_rows -%}
    ans = solve({{ actual_arguments }})
    {% if return_type == "bool" -%}
    for a in ans:
        print(YES if a else NO)
    {%- else -%}
    if ans:
        print(*ans, sep='\n')
    {%- endif %}
    {%- elif multiple_columns -%}
    ans = solve({{ actual_arguments }})
    {% if return_type == "bool" -%}
    print(*(YES if a else NO for a in ans))
    {%- else -%}
    print(*ans)
    {%- endif %}
    {%- else -%}
    ans = solve({{ actual_arguments }})
    {% if return_type == "bool" -%}
    print(YES if ans else NO)
    {%- else -%}
    print(ans)
    {%- endif %}
    {%- endif %}
{% endif -%}
{% else -%}
    # Failed to predict input format
    pass
{% endif %}
if __name__ == '__main__':
    main()
