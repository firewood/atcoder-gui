import sys
from typing import List, Optional

# Increase recursion depth for DFS/DP
sys.setrecursionlimit(2000000)

def input_tokens():
    for line in sys.stdin:
        for word in line.split():
            yield word

tokens = input_tokens()
{% set python_return_value_type = return_type %}
{% set python_output_value = "ans" %}
{% if mod %}
{% set python_return_value_type = "int" %}
MOD = {{ mod }}
{% elif return_type == "bool" %}
{% set python_output_value = "YES if ans else NO" %}
YES = "{{ yes_str }}"
NO = "{{ no_str }}"
{% elif return_type == "int" %}
{% if multiple_columns and multiple_rows %}
{% set python_return_value_type = "List[int]" %}
{% set python_output_value = "*ans" %}
{% endif %}
{% elif return_type == "string" %}
{% set python_return_value_type = "str" %}
{% endif %}
{% if multiple_columns or multiple_rows %}
{% set python_return_type = "List[" + python_return_value_type + "]" %}
{% else %}
{% set python_return_type = python_return_value_type %}
{% endif %}

{% if prediction_success %}
def solve({{ formal_arguments }}):
    ans: {{ python_return_type }}
{% if query_cases %}
    for _ in range({{ query_loop_var }}):
        t = int(next(tokens))
        if t == 1:
            pass
        elif t == 2:
            pass
        elif t == 3:
            pass
{% endif %}
    return ans
{% endif %}

# Generated by atcoder-gui {{ tools.version }} (tips: You use the default template now. You can remove this line by using your custom template)
{% if multiple_cases %}
def process_case():
{% else %}
def main():
{% endif %}
{% if prediction_success %}
{{ input_part | safe }}
{% if return_type == "void" %}
    solve({{ actual_arguments }})
{% elif multiple_rows %}
    answers = solve({{ actual_arguments }})
    for ans in answers:
        print({{python_output_value}})
{% elif multiple_columns %}
    answers = solve({{ actual_arguments }})
    print([{{python_output_value}} for ans in answers])
{% else %}
    ans = solve({{ actual_arguments }})
    print({{python_output_value}})
{% endif %}
{% else %}
    # Failed to predict input format
    pass
{% endif %}
{% if multiple_cases %}

def main():
    line = sys.stdin.readline()
    if not line:
        return
    t = int(line)
    for _ in range(t):
        process_case()
{% endif %}

if __name__ == '__main__':
    main()
